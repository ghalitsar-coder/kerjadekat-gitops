---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kerjadekat-frontend
  namespace: kerjadekat
  labels:
    app: kerjadekat-frontend
    version: latest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kerjadekat-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: kerjadekat-frontend
        version: latest
    spec:
      containers:
        - name: frontend
          # Jenkins CI updates this image tag on every successful build
          image: ghalitsar/kerjadekat-frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          envFrom:
            - configMapRef:
                name: kerjadekat-frontend-config
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
