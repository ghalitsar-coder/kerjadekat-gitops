---
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: kerjadekat
  labels:
    app: kerjadekat-frontend
spec:
  type: ClusterIP
  selector:
    app: kerjadekat-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
