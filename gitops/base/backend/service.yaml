---
apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: kerjadekat
  labels:
    app: kerjadekat-backend
spec:
  type: ClusterIP
  selector:
    app: kerjadekat-backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
