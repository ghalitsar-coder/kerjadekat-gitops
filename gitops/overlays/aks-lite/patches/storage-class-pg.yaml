# gitops/overlays/aks/patches/storage-class-pg.yaml
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
  namespace: kerjadekat-infra
spec:
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: default
